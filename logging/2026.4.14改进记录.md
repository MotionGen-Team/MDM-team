# 2026.4.14改进记录

## 目标
继续完成带轻量级 TCN 的 `trans_enc` 版本正式评测，将此前停在 `16/20` 的 `wo_mm` 任务在 GPU 上续跑至结束，并整理最终结果。

## 当天完成内容

### 1. 确认 TCN 正式评测当前断点状态
- 检查目录：
  - `checkpoints/tcn_trans_enc`
- 确认以下文件已存在：
  - `eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.log`
  - `eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.progress.json`
  - `eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.progress.pt`
- 读取进度文件后确认：
  - `completed_replications = 16`
  - `replication_times = 20`
- 说明：
  - TCN 的 `wo_mm` 正式评测此前已经具备断点文件
  - 当前不需要重头开始，只需从 `16/20` 继续跑完

### 2. 确认评测环境与 GPU 可用性
- 检查评测入口与设备选择逻辑：
  - `eval/eval_humanml.py`
  - `utils/parser_util.py`
  - `utils/dist_util.py`
- 确认评测命令支持通过 `--device` 指定 GPU 编号。
- 使用 `nvidia-smi` 检查设备状态，确认：
  - 可用 GPU 为 `GPU 0`
  - 启动前显存空闲
  - 启动前没有残留 Python 评测进程占用 GPU

### 3. 用 GPU 续跑 TCN 的 `wo_mm` 正式评测
- 实际续跑命令等价于：
  ```powershell
  python -m eval.eval_humanml --device 0 --model_path checkpoints\tcn_trans_enc\model000021448.pt --eval_mode wo_mm
  ```
- 本次续跑的独立输出日志为：
  - `checkpoints/tcn_trans_enc/eval_resume_20260413_192827_stdout.log`
  - `checkpoints/tcn_trans_enc/eval_resume_20260413_192827_stderr.log`
- 主评测日志与进度文件仍继续写回：
  - `checkpoints/tcn_trans_enc/eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.log`
  - `checkpoints/tcn_trans_enc/eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.progress.json`
  - `checkpoints/tcn_trans_enc/eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.progress.pt`

### 4. 确认 TCN 正式评测已完成
- 复查主日志后确认：
  - 本次续跑已完成 `Replication 18` 和 `Replication 19`
  - 评测总进度已更新为 `20/20 replications`
  - 日志末尾已输出完整 summary
- 进度文件中也已确认：
  - `completed_replications = 20`
  - `mean_dict` 已写入最终汇总结果

### 5. 记录 TCN `wo_mm` 最终结果
- 最终 `vald` 汇总结果为：
  - `Matching Score: 18.3252`
  - `R_precision(top1/top2/top3): 0.0359 / 0.0714 / 0.1004`
  - `FID: 2.6069`
  - `Diversity: 8.4722`
- 对应结果来源文件：
  - `checkpoints/tcn_trans_enc/eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.log`
  - `checkpoints/tcn_trans_enc/eval_humanml_tcn_trans_enc_000021448_gscale2.5_wo_mm.progress.json`

### 6. 与 baseline 正式评测结果做直接对比
- baseline `vald` 结果（记录于 `logging/2026.4.7改进记录.md`）：
  - `Matching Score: 18.8635`
  - `R_precision(top1/top2/top3): 0.0289 / 0.0570 / 0.0865`
  - `FID: 4.1669`
  - `Diversity: 8.5360`
- TCN `vald` 结果：
  - `Matching Score: 18.3252`
  - `R_precision(top1/top2/top3): 0.0359 / 0.0714 / 0.1004`
  - `FID: 2.6069`
  - `Diversity: 8.4722`
- 当前结果解读：
  - `Matching Score` 有所下降
  - `R_precision` 三个 top-k 指标均优于 baseline
  - `FID` 明显优于 baseline
  - `Diversity` 与 baseline 接近，略低一些

### 7. 记录本次续跑过程中的附带现象
- 本次续跑的 `stderr` 末尾仍出现 WandB 临时目录清理相关报错：
  - `PermissionError: [WinError 5]`
- 报错位置仍然发生在程序退出时的 `atexit` 清理阶段。
- 实际影响判断：
  - 不影响本次 `wo_mm` 评测完成
  - 不影响 summary 输出
  - 不影响进度文件与最终结果保存

## 当前状态
- baseline `wo_mm` 正式评测：
  - 已完成
  - 已有最终结果
- TCN `wo_mm` 正式评测：
  - 已完成
  - 已从 `16/20` 断点续跑至 `20/20`
  - 已得到最终汇总结果

## 后续建议
- 将 TCN 与 baseline 的正式评测结果整理成一份对比结论，便于后续写实验分析。
- 如果后续重点看生成质量，可优先继续：
  - `mm_short`
  - 可视化采样结果对比
- 如果后续重点看结构收益，可继续尝试：
  - 更多 TCN dilation 组合
  - 更浅或更深的 TCN 层数配置
