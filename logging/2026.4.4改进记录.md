# 2026.4.4改进记录

## 目标
将原本偏可视化的对比思路，转为采用 MDM 官方评测指标进行模型对比，并为长时间评测补充可恢复的进度保存能力。

## 当天完成内容

### 1. 梳理当前对比方案
- 确认 `generate_npy/refined.npy` 是在原始结果后额外过一个 CNN 得到的后处理结果，不适合作为正式模型对比依据。
- 确认后续实验应以 MDM 官方指标为主：
  - FID
  - R_precision
  - Diversity
  - Multimodality

### 2. 确认 baseline 模型目录
- 将可用的复现 baseline 模型整理到：
  - `checkpoints/baseline_trans_enc`
- 后续公平对比基线确定为：
  - `checkpoints/baseline_trans_enc/model000021448.pt`
- 官方 `save/humanml_trans_enc_512` 目录保留为参考模型，不作为公平主对比对象。

### 3. 补齐官方 evaluator 资源
- 确认 `eval/eval_humanml.py` 依赖 T2M evaluator。
- 缺失文件路径确认为：
  - `t2m/text_mot_match/model/finest.tar`
- 后续已将官方 evaluator 资源放置到仓库根目录下的：
  - `t2m/`
  - `kit/`

### 4. 修改评测脚本，支持断点续跑
- 修改文件：
  - `eval/eval_humanml.py`
- 新增能力：
  - 每完成一个 replication 自动保存进度
  - 保存当前完成的 replication 数
  - 保存当前累计指标
  - 下次运行同一命令时自动从断点继续
- 生成的进度文件形式：
  - `*.progress.json`
  - `*.progress.pt`
- 所有当天新增或替换的关键代码均按要求标记了 `# G`
- 原有相关逻辑未直接删除，必要位置保留为注释并标 `G`

### 5. 新增轻量流程验证模式
- 修改文件：
  - `utils/parser_util.py`
  - `eval/eval_humanml.py`
- 新增评测模式：
  - `quick_debug`
- 作用：
  - 只验证评测流程、日志保存、进度保存、续跑能力
  - 不跑 FID / Diversity / Multimodality
  - 仅保留 Matching Score 与 R_precision 的流程验证

### 6. 修复 Windows 下评测 DataLoader 权限问题
- 修改文件：
  - `data_loaders/humanml/motion_loaders/model_motion_loaders.py`
- 调整内容：
  - 将评测相关 DataLoader 的 `num_workers` 改为 `0`
- 目的：
  - 避免 Windows 环境下出现 `PermissionError: [WinError 5]`
- 原始 `num_workers=4/1` 代码已注释保留，并标记 `G`

### 7. 完成 quick_debug 验证
- `quick_debug` 已成功跑通
- 已确认以下文件可正常生成：
  - `eval_humanml_baseline_trans_enc_000021448_gscale2.5_quick_debug.log`
  - `eval_humanml_baseline_trans_enc_000021448_gscale2.5_quick_debug.progress.json`
  - `eval_humanml_baseline_trans_enc_000021448_gscale2.5_quick_debug.progress.pt`
- 说明：
  - 日志保存正常
  - 进度保存正常
  - 断点恢复机制已生效

## 当前状态
- baseline 正式评测命令已切换为：

```powershell
python -m eval.eval_humanml --model_path checkpoints\baseline_trans_enc\model000021448.pt --eval_mode wo_mm
```

- `wo_mm` 仍然是长时间任务，但现在已经具备断点保存与续跑能力。

## 后续建议
- 先完成 baseline 的 `wo_mm` 正式评测并保存结果
- 改模型训练完成后，使用相同训练预算和相同评测脚本进行对比
- 主对比表建议使用：
  - FID
  - R_precision
  - Diversity
- Multimodality 可作为补充实验单独报告
