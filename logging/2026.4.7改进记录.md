# 2026.4.7改进记录

## 目标
在 baseline `wo_mm` 正式评测完成后，整理今天的实验进展，并启动带轻量级 TCN 的 `trans_enc` 版本正式评测。

## 当天完成内容

### 1. 确认 baseline `wo_mm` 正式评测已完成
- 复查日志文件：
  - `checkpoints/baseline_trans_enc/eval_humanml_baseline_trans_enc_000021448_gscale2.5_wo_mm.log`
- 确认该日志已完成全部 `20/20 replications`，并输出最终 summary。
- 同时检查当前系统进程，确认没有仍在运行的 baseline Python 评测进程。
- baseline `vald` 最终汇总结果为：
  - `Matching Score: 18.8635`
  - `R_precision(top1/top2/top3): 0.0289 / 0.0570 / 0.0865`
  - `FID: 4.1669`
  - `Diversity: 8.5360`

### 2. 补齐 TCN 开关的训练参数透传
- 虽然 `model/mdm.py` 中已经实现了 `LightweightTemporalTCN`，但原训练命令行还不能直接打开该分支。
- 修改文件：
  - `utils/parser_util.py`
  - `utils/model_util.py`
- 修改内容：
  - 在模型参数中新增 `--use_temporal_tcn`
  - 在 `get_model_args(...)` 中将 `use_temporal_tcn` 传入 `MDM(...)`
- 修改目的：
  - 训练时可以直接通过命令行开启 TCN
  - `args.json` 会保存该开关
  - 后续采样与评测在 `--model_path` 加载配置时也能自动恢复该结构

### 3. 验证 TCN 开关已接通
- 使用如下命令检查训练入口帮助信息：
  ```powershell
  python -m train.train_mdm --help
  ```
- 结果：
  - 已能正确看到 `--use_temporal_tcn`
  - 说明训练参数解析已生效

### 4. 启动并完成 TCN 版本训练
- 启动命令：
  ```powershell
  python -m train.train_mdm --save_dir checkpoints\tcn_trans_enc --dataset humanml --arch trans_enc --layers 8 --latent_dim 512 --batch_size 16 --cond_mask_prob 0.1 --lr 0.0001 --save_interval 1000 --num_steps 20000 --overwrite --use_temporal_tcn
  ```
- 训练目录：
  - `checkpoints/tcn_trans_enc`
- 训练配置与 baseline 对齐：
  - `dataset=humanml`
  - `arch=trans_enc`
  - `layers=8`
  - `latent_dim=512`
  - `batch_size=16`
  - `num_steps=20000`
- 当前训练产物已正常生成：
  - `checkpoints/tcn_trans_enc/model000021448.pt`
  - `checkpoints/tcn_trans_enc/opt000021448.pt`
  - `checkpoints/tcn_trans_enc/args.json`
  - `checkpoints/tcn_trans_enc/train_stdout.log`
  - `checkpoints/tcn_trans_enc/train_stderr.log`
- `args.json` 中已确认：
  - `use_temporal_tcn: true`

### 5. 记录训练过程中的附带现象
- `train_stderr.log` 末尾出现了 WandB 临时目录清理相关的 `PermissionError: [WinError 5]`
- 该报错发生在训练结束后的 `atexit` 清理阶段
- 实际影响判断：
  - 不影响 checkpoint 保存
  - 不影响本次训练完成
  - 不影响后续基于 `model000021448.pt` 的正式评测

### 6. 准备启动 TCN 版本正式评测
- 评测目标模型：
  - `checkpoints/tcn_trans_enc/model000021448.pt`
- 计划使用与 baseline 完全一致的正式评测命令形式：
  ```powershell
  python -m eval.eval_humanml --model_path checkpoints\tcn_trans_enc\model000021448.pt --eval_mode wo_mm
  ```
- 预期输出内容包括：
  - `Matching Score`
  - `R_precision`
  - `FID`
  - `Diversity`
- 评测日志和断点文件将自动保存在 `checkpoints/tcn_trans_enc/` 下，便于与 baseline 直接对比。

## 当前状态
- baseline `wo_mm` 正式评测：
  - 已完成
  - 已得到最终汇总结果
- TCN 版本训练：
  - 已完成
  - 最终模型已保存为 `checkpoints/tcn_trans_enc/model000021448.pt`
- TCN 版本正式评测：
  - 下一步立即启动

## 后续建议
- 先让 TCN 版本 `wo_mm` 正式评测完整跑完
- 拿到完整 summary 后，与 baseline 做逐项对比：
  - Matching Score
  - R_precision
  - FID
  - Diversity
- 如果 TCN 结果有收益，再考虑继续做：
  - `mm_short`
  - 生成结果抽样可视化
  - 更多 TCN 结构或 dilation 配置实验
